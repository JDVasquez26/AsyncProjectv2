import React from 'react'
import { useSelector, useDispatch } from 'react-redux';
import { authenticate } from '../../features/authFeatures/authSlice';

const SingUpForm = () => {
    const dispatch = useDispatch();

    
  return (
    <div>SingUpForm</div>
  )
}

export default SingUpForm